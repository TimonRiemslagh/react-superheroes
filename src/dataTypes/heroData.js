export default class HeroData {
    constructor(id, avatar, name, powers, description) {
        this.id = id;
        this.avatar = avatar;
        this.name = name;
        this.powers = powers;
        this.description = description;
    }
}